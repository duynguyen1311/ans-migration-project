version: '3'

services:
  ans-migration:
    build: .
    container_name: ans-migration
    volumes:
      - ./logs:/usr/src/app/logs
      - ./config.js:/usr/src/app/config.js
      - ./credential.json:/usr/src/app/credential.json
    restart: unless-stopped